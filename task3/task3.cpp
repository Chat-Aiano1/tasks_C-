#include <iostream>
#include <iomanip> // для використання setw
using namespace std;

int main() {
    int m, n;

    // Зчитування вхідних даних
    cout << "Введіть кількість рядків (m): ";
    cin >> m;
    cout << "Введіть кількість стовпців (n): ";
    cin >> n;

    // Перевірка на коректність введених даних
    if (m < 1 || m > 100 || n < 1 || n > 100) {
        cout << "Неправильні вхідні дані! m і n повинні бути в діапазоні від 1 до 100." << endl;
        return 1; // Завершення програми з помилкою
    }

    // Заповнення та виведення прямокутника
    int number = 0; // Початкове значення числа
    for (int i = 0; i < m; i++) { // Цикл для рядків
        for (int j = 0; j < n; j++) { // Цикл для стовпців
            cout << setw(3) << number; // Виведення числа з фіксованою шириною
            number++; // Збільшення числа на 1
        }
        cout << endl; // Перехід на новий рядок після завершення рядка
    }

    return 0;
}
